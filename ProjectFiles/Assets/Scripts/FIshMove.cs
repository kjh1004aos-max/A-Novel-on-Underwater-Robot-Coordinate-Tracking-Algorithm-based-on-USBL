using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class FIshMove : MonoBehaviour
{

    /*

    // Update is called once per frame
    void Update()
    {
        PosChange();
    }
    public void PosChange()
    {
        SerialCom_1 serial = GameObject.Find("Serial").GetComponent<SerialCom_1>();
        float x = Convert.ToSingle(serial.slavePos[0]);
        float y = Convert.ToSingle(serial.slavePos[1]);
        float z = Convert.ToSingle(serial.slavePos[2]);
        transform.position = new Vector3(x, y, z);*/
    
}
